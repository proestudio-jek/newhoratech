rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // REGRAS DE SEGURANÇA - PROMUSIC
    // Gatilho de Implantação: 2025-05-26 15:45 (GMT)
    // Objetivo: Resolver definitivamente erros de 'list' e 'get' para visitantes e usuários logados.
    
    function isSignedIn() {
      return request.auth != null;
    }

    // Coleções com LEITURA PÚBLICA (Permite 'get' e 'list' para todos)
    // O uso de {document=**} garante que subcoleções e listagens sejam permitidas.
    
    match /community-news/{document=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    match /community-playlist/{document=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    match /community-videos/{document=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    match /calendarEntries/{document=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    match /hymns/{document=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    // Hinos de solistas: Leitura pública, escrita apenas para o autor logado.
    match /solistaHymns/{document=**} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && (
        resource == null || resource.data.solistaId == request.auth.uid
      );
    }

    // Perfil de usuário: Acesso restrito ao próprio dono.
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }

    // Bloqueio padrão para segurança de coleções não especificadas.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}